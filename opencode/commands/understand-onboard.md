---
description: Generate an onboarding guide for new team members joining a project. Wraps the `understand-onboard` skill.
---

# /understand-onboard — Onboarding guide generator (skill wrapper)

Load the `understand-onboard` skill via the `skill` tool, then execute it with the user's arguments.

## Invocation

1. Call `skill({ name: "understand-onboard" })` to load the workflow.
2. Treat the following as `$ARGUMENTS`: `$ARGUMENTS`
3. Follow the skill's instructions exactly.
